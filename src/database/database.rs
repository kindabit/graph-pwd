/// A collection of plain (unencrypted) data of accounts and other necessary auxiliary data,
/// which will be encrypted before being save to filesystem,
/// and be decrypted to after being read from filesystem.
pub struct Database {

}
